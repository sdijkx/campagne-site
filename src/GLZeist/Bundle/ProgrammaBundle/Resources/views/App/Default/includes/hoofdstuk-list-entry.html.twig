<article class="list-item">
    <h2 class="titel">
        <a title="{{hoofdstuk.titel}}" href="{{path('hoofdstuk',{slug:hoofdstuk.slug})}}">{{hoofdstuk.titel}}</a>
    </h2>
    <div class="tekst">
        {{hoofdstuk.samenvatting|raw}}
    </div>
</article>