{% extends "GLZeistProgrammaBundle::admin.html.twig" %} {%block content%}<h1>Paragraaf edit</h1>

<form action="{{ path('paragraaf_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('paragraaf') }}">
            Terug naar het overzicht
        </a>
    </li>
    <li>
        {{macros.delete_form('paragraaf_delete',delete_form,entity)}}
    </li>
</ul>
{%endblock%}