{% extends "GLZeistProgrammaBundle::admin.html.twig" %} {%block content%}<h1>Trefwoord list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Trefwoord</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('trefwoord_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.trefwoord }}</td>
            <td>
                <ul  class="actions">
                    <li>
                        <a href="{{ path('trefwoord_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('trefwoord_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul class="actions">
    <li>
        <a href="{{ path('trefwoord_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{%endblock%}