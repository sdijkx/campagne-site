{% extends "GLZeistProgrammaBundle::admin.html.twig" %} {%block content%}<h1>Relatie edit</h1>

<form action="{{ path('relatie_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('relatie') }}">
            Terug naar het overzicht
        </a>
    </li>
    <li>
        <form action="{{ path('relatie_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Verwijderen</button>
        </form>
    </li>
</ul>
{%endblock%}