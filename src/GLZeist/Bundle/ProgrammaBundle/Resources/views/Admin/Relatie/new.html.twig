{% extends "GLZeistProgrammaBundle::admin.html.twig" %} {%block content%}<h1>Relatie creation</h1>

<form action="{{ path('relatie_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('relatie') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endblock%}