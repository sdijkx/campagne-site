{% extends "GLZeistProgrammaBundle::admin.html.twig" %} 
{%block content%}
<h1>Nieuw Hoofdstuk</h1>

<form action="{{ path('hoofdstuk_new') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Nieuw</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('hoofdstuk') }}">
            Terug naar het overzicht
        </a>
    </li>
</ul>
{%endblock%}